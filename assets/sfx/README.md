# Sound effects (placeholder)

Short procedural sine tones generated via ffmpeg for wiring and QA. Replace with
licensed gameplay SFX when art audio is ready.

Files match `JuiceManager.SFX` paths in `feedback/JuiceManager.gd`.

Toggle: Settings → **Sound Effects** (`sfx_enabled` in progression save). Master
mute on the menu still silences all output.
